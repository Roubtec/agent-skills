interface:
  display_name: "Address Tasks"
  short_description: "Implement task batches in parallel"
  default_prompt: "Use $address-tasks to implement <task-numbers-paths-or-globs> in parallel with worktrees."
