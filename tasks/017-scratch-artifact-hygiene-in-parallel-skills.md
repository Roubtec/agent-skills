# 017 — Scratch-artifact hygiene for parallel runs: unique names, worktree-local paths, post-batch cleanliness

## Why this task exists

Concurrent subagents in one session share a single scratchpad directory, and the parallel skills have silently bled data across isolation boundaries three separate times:

- two parallel `address-review` fixers both wrote review threads to a fixed `threads.json`; one triaged with the other PR's data and was saved only by the per-comment URL scope check (jabko, 6-PR batch; recurred in a kalm2 batch);
- two parallel reviewers both redirected `pnpm verify` output to `<scratchpad>/verify.log`; one read the other worktree's results and reported a validation verdict for the wrong branch — caught only because the file count didn't match (Scribz);
- the Scribz incident was then **misdiagnosed** as a working-directory bug, and the wrong mitigation propagated to ~10 subsequent subagent prompts before being root-caused to the filename collision.

Separately, `address-tasks` batches have leaked stray files into the shared main checkout (an implementer wrote to the repo root instead of its worktree; the strays survived to the end of the run), and nothing in the skill asserts the main checkout ends clean.

A fourth failure, of a different class, arrived later: a reviewer subagent authorized to verify a claim empirically ran `rm -rf ./*` in the shared main checkout. Its setup step — `git clone … 2>&1 | tail -2` — hid the clone's failure from `set -e`, because a pipeline's status is its last command, so execution continued with the working directory still at the repo root. Tracked files were deleted and a branch ref was moved onto an orphan commit; nothing was lost only because `./*` does not match dotfiles, so `.git` survived. The three failures above are about artifacts *colliding*; this one is about state being *destroyed*, and no rule in this task as originally written would have prevented it.

## Scope

Included:

1. **Unique-name rule** in every skill that runs parallel subagents (`address-reviews`, `address-tasks`, and `address-review` when invoked as a batch member): any scratch artifact a subagent writes outside its own worktree MUST be namespaced by its task identity — PR number, worktree slug, or `$$` — or live in a per-task subdirectory the orchestrator hands it. Name the two observed collisions (`threads.json`, `verify.log`) as the canonical counterexamples. Prefer redirecting validation output to a file **inside the assigned worktree** (gitignored or cleaned before commit) over the shared scratchpad.
2. **`address-review` internal artifacts**: everywhere the skill's own recipe names a scratch file (review threads dump, verify logs, prompt files), bake the PR number into the documented filename (`threads-<PR#>.json`) so even single-PR guidance is collision-proof by default. The URL scope-check stays as backstop, not primary defense.
3. **Post-batch cleanliness assertion** in `address-tasks`/`address-tasks-serialized`: once every batch entry has reached **any terminal state**, the orchestrator reads the main checkout's working-tree state and compares it against a reading of the same shape taken at batch start; keyed by path, so that a status code changing under one path reads as a re-classification rather than as one path vanishing and another appearing; paths that appeared are a loud finding in the final report, paths that disappeared are a louder one — a stray `checkout`/`clean` in the shared tree eating a co-tenant's uncommitted work is the hazard the implementation notes warn about, and only a baseline can see it — though reported as something to check against co-tenant commits rather than as established loss, since a maintainer committing their own work mid-run also removes a baseline path and nothing is gone. The rest are reported as pre-existing (diff strays against delivered branches before touching them — another agent's uncommitted work is not ours to delete). Take a baseline rather than testing for emptiness: the bootstrap deliberately permits the maintainer's own uncommitted work in the main checkout, so an unconditional non-empty rule both blames the batch for work it never touched and destroys the one signal that tells a real stray from the tree's starting state. Specify what the assertion **claims** rather than how to compute it. The obvious computation — diffing two `git status --porcelain` outputs line by line — misreports in both directions, in more ways than this spec can usefully close, and successive review rounds each found a fresh one: a path that already carried an entry at bootstrap absorbs any volume of later writes behind an identical line, ignored paths lie outside `status` altogether while item 1 above actively steers scratch output into them, and a maintainer staging or committing mid-run rewrites baseline lines with nothing lost at all. So state the rule as an **exclusion rather than an enumeration**: the check reports what it can see change, and claims nothing about *what was written to* paths already present in the baseline, nor about paths invisible to the reading it takes. Reporting that a baseline-present path was re-classified is an observation it can make; asserting that path was left alone is not. Bound it in the other direction too: what it does surface is a report to verify, not a conclusion, because the same reading cannot tell a stray from a co-tenant's ordinary progress. It is a report obligation, never a proof that the batch wrote nothing, and must not be delivered as one. An implementer who widens what it can see — listing untracked files individually, extending the reading to ignored paths — improves the report and must leave that claim exactly as narrow. Trigger it unconditionally on batch termination, not on "the last task delivers": a batch where every task blocks, fails, or aborts never reaches a delivery, so gating on one skips the check exactly when it matters most — a failed implementer is at least as likely to have leaked strays, and the report would otherwise close out clean without surfacing them.
4. **Diagnosis discipline** one-liner in the batch skills: a subagent's environment/infrastructure diagnosis is a hypothesis, not a finding — verify against its transcript (bounded greps for the specific commands it claims it ran) before propagating any mitigation to sibling prompts.
5. **Subagent write/destroy boundary** in every place that spawns a subagent — the seven spawning skills (`address-review`, `address-reviews`, `address-tasks`, `address-tasks-serialized`, `resolve-open-questions`, `review-cycle` — the canonical building block the five before it delegate their role contracts to — and `reap-tasks`, which tells the orchestrator to consider parallel verification subagents for disparate tasks (`SKILL.md:18` in both mirrors) and so spawns exactly the empirical repository inspection this item exists to bound, while composing no brief of its own, so its boundary is stated in the skill text the orchestrator itself follows rather than amended into a template) with their Codex mirrors, plus all three dynamic workflows — `wf-review-cycle.js`, which composes the fixer, reviewer, and peer briefs at the four `cycleContract()` call sites, plus its own `scopePrompt` (`:1083`, rendered at `:1150`) which sits *outside* the embeddable section (`:114-1064`) and so is reached by nothing placed at those call sites; `wf-address-tasks.js`, which embeds that composition byte-for-byte as its `review-cycle-core` section (`:461-1411`) and composes eight further briefs of its own outside it — `bootstrapPrompt` (`:194`), `storageProbePrompt` (`:211`), `mainCheckoutStatusPrompt` (`:233`), `resolvePrompt` (`:390`), `prPrompt` (`:1456`, which returns two different bodies for the remote and no-remote cases), `cleanupNote` (`:1471`), `collisionScanPrompt` (`:1477`), `resolveCollisionsPrompt` (`:1522`), several of which point a subagent at mutating git or `gh` work in the shared tree and so need the boundary most — `cleanupNote` most of all, since it sends the subagent *out of its worktree to the repo root* to run `wt-remove`; and `wf-address-review.js`, which nests the cycle as a sub-workflow and composes its own `gatherPrompt` (`:188`) and `publishPrompt` (`:250`) besides. Scope-delta item 4 below records how that list grew, and which placements actually reach a subagent.

   **Treat that enumeration as a floor, not a ceiling, and re-derive it before delivering.** It is a snapshot of a moving tree — three successive review rounds each extended it and each was still incomplete — so the binding instruction is the derivation, not the list: at implementation time, find *every* site in each file that builds text reaching a subagent (in the workflows, every `*Prompt`/note function whose result is passed to `agent()` or written to a peer prompt file; in the skills, every prompt template or brief a spawn instruction hands over — **and every spawn instruction that hands over none**, which needs the boundary in the skill text the orchestrator itself follows, since there is no template to carry it) and cover all of them. That second shape is the one a derivation keyed to templates passes over, and `reap-tasks` is exactly it. A path discovered that way and absent from the list above is in scope, not an exception to it. Not only the parallel skills: the incident happened in a single-PR `address-review` run, and the hands-off `wf-address-review` path is the closest analogue to it, so leaving the workflows out would exempt the most exposed caller. Do not extend it to `rebase-stack`, which issues no spawn instruction of any kind — only a note about being *called by* a subagent (`:71`) — or to `enable-worktrees`, which explicitly forbids spawning (`:10`) — adding a prompt contract there would contradict the skill's own rule. Items 1 to 3 say where a subagent should put its artifacts; this one says what it may destroy, and where. The boundary text — the prompt contract where a brief is handed over, the skill's own spawn instruction where none is — must state the permitted read-only command set and name the forbidden one explicitly, including that a destructive command stays forbidden "not in a clone, not in a temp directory, not 'safely'" — the qualifier matters, because the incident's subagent believed it *was* working in a clone. It must also state the isolation boundary the incident turned on: a worktree isolates the working tree, not the repository, so `branch -f`, `reset`, `update-ref`, and `gc` reach every sibling worktree, and a worktree is never a safe blast radius for anything mutating refs or objects. This item owns that wording, including the degradation clause that sends empirical verification to the disposable-clone helper of task 020 when `command -v` finds it and to an absolute path outside the repository — never a relative one — when it does not; 020 verifies that pointer rather than re-authoring it, so either task may land first. Prose is the weakest of the available layers and is known not to be sufficient alone; it is in scope here because it is the part that belongs to the skills, with the helper in 020 and the container-side controls carrying the rest.

Out of scope:

- Harness-level per-subagent scratchpad namespacing (upstream Claude Code concern).
- powbox container-doc wording (tracked as powbox task 045).
- A destroy boundary for `rebase-stack`'s own behaviour. It runs `git reset --hard` and `git clean -fd` on disposable branches, which is a rule about what it does when invoked rather than a contract it writes for someone else, so it does not belong in item 5 and is not folded in here. Whether it deserves a task of its own is unfiled and undecided. Conflating the two kinds of boundary is what produced an earlier draft's wrong skill list.

## Scope delta found by the 2026-08-06 reap sweep

This task was branched but never implemented: PR #37 carries a stub commit and an unrelated archival commit, and touches nothing under `plugins/` or `codex/`. It stays queued. The codebase moved underneath it in the meantime, so five things have changed about what delivering it means. This section is additive in intent — every item above still stands — with three exceptions recorded here rather than left to inference, all to item 5's lists: its workflow list has been corrected in place from two workflows to three and its skill list from five to six, both per item 4 below; and its skill list again from six to seven, adding `reap-tasks`, whose parallel verification subagents are an ordinary spawning path that item 5's derivation rule — keyed to prompt templates — could not have surfaced, since that skill spawns without composing one. That rule now covers the briefless shape too. Item 5 also gained the individual prompt paths within each workflow and the floor note governing them, and the "Target files or areas", "Acceptance criteria", and "Review plan" sections have been updated to match.

1. **Items 1 and 2 are already satisfied, incidentally, by other work.** No fixed-name scratch file survives in either skill tree (`threads.json` and `verify.log` return no hits), and the batch skills already require unique per-invocation artifact paths — `address-tasks/SKILL.md:122`, `address-reviews/SKILL.md:166`, `review-cycle/SKILL.md:100-102`. The literal `threads-<PR#>.json` never appeared and is moot: the thread-fetch recipe now streams `gh-review-threads` to stdout, so there is no dump file left to namespace. What remains unaddressed under item 1 is narrow, and it is one thing arriving by several routes: nothing tells a reviewer where its **build and validation output** goes, which is the exact `verify.log` shape the item exists to prevent. Three routes carry it today, and they need different fixes because they deliver the instruction differently.

   - **Inline templates**, which inherit nothing: `address-tasks/SKILL.md:196-201` spells its reviewer prompt out and omits the destination at the build instruction (`:199`), and `address-tasks-serialized/SKILL.md:162` does the same. The serialized one is in scope despite running no batch concurrently, because the scratchpad a collision happens in is shared per session rather than per batch.
   - **An inherited contract**: `address-reviews` Phase B spawns one own Reviewer per entry concurrently (`address-reviews/SKILL.md:160-166`), and the per-entry path namespacing at `:166` governs only the separate peer's prompt and output. The Reviewer's own contract arrives through `:162` — "Use `address-review` step 6's reviewer contract" — which `address-review` in turn delegates to `review-cycle/SKILL.md:19`, where the build is required and no destination is assigned. That hop is worth following rather than assuming: it is what makes one edit to the shared contract reach every consumer of the cycle. The unique artifact directory at `review-cycle/SKILL.md:100-102` does not already close it, since that directory is scoped to the round history it enumerates, not to output a reviewer redirects while running the build.
   - **A briefless spawn instruction**, which no contract edit reaches: `reap-tasks` tells the orchestrator to consider parallel verification subagents (`SKILL.md:18`) and tells each to run a full build (`:32`) while composing no brief at all — the gap opens the moment that option is taken. Exactly as item 5 found for the destroy boundary, its text belongs in the skill the orchestrator itself reads. It is also the most exposed of the three, having no worktree isolation to fall back on.

   **Treat those three as a floor and re-derive before delivering**, on item 5's rule and for its reason: this list read as one path until a review round found the second and third. The derivation is every place a subagent is told to run a build or validation, by whichever of the three routes. Cover the codex mirror of each file touched.

2. **Item 3 is delivered in the workflow, but not on every termination, and not in the skills at all.** `wf-address-tasks.js` has the shape item 3 asks for — baseline at `:1652`, post-batch read under `phase("Summary")` at `:1969-1974`, path-keyed comparison, vanished-baseline surfacing, observe-only. What it does not have is the unconditional trigger: an unresolvable or empty plan returns at `:1660-1661`, after the baseline and the resolve subagent have already run, so that invocation exits without the comparison — a zero-delivery termination of exactly the kind item 3 names, reported clean because it was never reported at all. The fix is bounded: `:1661` is the only return between the baseline and the Summary (`:1639`'s bootstrap failure precedes the baseline, so there is nothing yet to compare against), so that one path needs the comparison before it returns. None of the check exists in `address-tasks/SKILL.md`, `address-tasks-serialized/SKILL.md`, or either codex mirror, which is what a non-workflow run reads. Deliver item 3 into those four files, paraphrasing the workflow rather than re-deriving it, and phrase the trigger so it binds an early return as squarely as a completed batch.

3. **Item 4's claim bound is not fully honoured even in the workflow.** Two gaps to close while delivering item 3: `wf-address-tasks.js:361`'s unchanged-case note reads as a completeness claim over pre-existing paths and omits the `OTHER_STAGES` disclaimer; and the invisible-reading exclusion appears at `:227` as a closed enumeration ("ignored paths like `.worktrees/` never appear") rather than the open exclusion this task requires.

4. **`wf-review-cycle.js` is a third prompt-composing site that did not exist when item 5's list was written.** Task 014 extracted the review cycle, and `cycleContract(cycle, role)` (`wf-review-cycle.js:340`) is now the shared brief behind the fixer, reviewer, and peer prompts. It is currently the weakest of the three workflows — it lacks even the shared-tree `reset --hard` / `clean -fdx` ban that `wf-address-tasks.js:444` carries. Item 5's own list, written before that workflow existed, originally named two workflows and has been corrected in place to **three**, per the note opening this section; the requirement is three, however few edits deliver it.

   The same commit left item 5's *skill* list one short, for exactly the same reason. `4bdf17e` added `wf-review-cycle.js`, `plugins/dev-skills/skills/review-cycle/SKILL.md`, and its Codex mirror together; this delta originally caught only the workflow half. `review-cycle/SKILL.md` composes subagent prompts itself — `:16` spawns three roles freshly each round, `:19` briefs the Reviewer — and all five originally listed skills now delegate their role contracts to it, as `write-tasks` does by default. Adding the boundary to five skills while skipping the canonical one they all point at is the skill-side twin of the `cycleDefaultContract()` trap below. Item 5's skill list is corrected in place to **six** with their mirrors.

   **Placement decides whether it propagates, so choose it deliberately.** `cycleContract()` resolves a per-role override first and falls back to `cycleDefaultContract()` (`:332`) only when the consumer supplies none — and `wf-address-tasks.js` supplies **all three** roles (`contracts: { fixer, reviewer, peer }` at `:1443-1447`, built by `worktreeContract()` at `:426`, both of which sit *outside* its embedded section, which spans `:461-1411`). So `cycleDefaultContract()` is dead code for every `wf-address-tasks` subagent, and boundary text placed there would propagate byte-for-byte into the embedded section while reaching none of that workflow's prompts. Cite the whole trap rather than only its conclusion: the byte-identity check would still pass, so the section-diff validation cannot catch this.

   The placement that reaches every **workflow-runtime** agent is the **prompt bodies at the four `cycleContract()` call sites** — `:402` (fixer), `:463` (reviewer), `:499` (peer), `:581` (the peer-finding grounding spot-check, which passes the *reviewer* contract — it is not a re-review; re-review rounds go back through `:463`) — since those render for all three workflows regardless of whether a consumer overrides its contracts. Say "workflow-runtime agent" rather than "every consumer", because one recipient sits outside that set: `:499` briefs the peer *coordinator*, and the text the codex peer itself receives is the separate verbatim block the coordinator copies to a prompt file (`:520` onward, mirrored at `wf-address-tasks.js:867`). Nothing placed at `:499` reaches it. That block is deliberately **out of scope** for this item, and the reason is worth stating so nobody re-adds it later: the peer is launched `codex exec --sandbox read-only` (`:512`), so its write boundary is enforced by the sandbox rather than by prose — strictly stronger than the contract item 5 writes, and this task's own note says prose is the weakest available layer. Adding a destroy boundary to a process that is already incapable of destroying would be a case added, not a hazard closed. An implementer may still prefer a different arrangement; what this delta requires is that the choice be verified by reading a rendered prompt from each workflow, not inferred from the embed relationship. Note also that `wf-address-review.js` supplies no contracts, so it *does* fall back to `cycleDefaultContract()` — placing the text there would cover two of the three workflows and silently skip the batch one, which is the most exposed.

5. **Item 5's degradation clause can now name the real helper interface.** Task 020 landed `dc-enter` and `dc-remove`, and nothing in any skill or workflow yet points at them. The pointer should match what shipped: `dc-enter <slug> [<ref>]` prints one absolute path and nothing else on stdout (diagnostics go to stderr, so `DC="$(dc-enter probe)"` stays clean), `<ref>` defaults to the *invoking worktree's* HEAD, and `dc-remove <slug>` takes a slug, never a path. One behaviour the wording must account for: **a reused slug is refused, not silently re-derived** — guidance that may run twice needs `--replace` or a `dc-remove <slug>` cleanup step, or it will fail on its second invocation.

## Context and references

- `plugins/dev-skills/skills/address-review/SKILL.md` — thread-fetch and validation recipes naming scratch files.
- `plugins/dev-skills/skills/address-reviews/SKILL.md`, `address-tasks/SKILL.md`, `address-tasks-serialized/SKILL.md` — orchestrator prompt templates where the rule and the cleanliness assertion belong.
- `codex/dev-skills/skills/*` — mirror where the same recipes exist.
- `agent-session-learnings-20260730-231224.md` on branch `origin/learnings/session-20260730-231224` (commit `f9b9c16`) — items 1 to 4 there record the destruction incident, the worktree-isolation boundary, and the `set -e`-plus-pipeline trap that turned a careless script into a destructive one. Read cross-branch with `gitcat`, using the `origin/` prefix or the commit sha; the branch may not exist locally. It carries no other change.
- 020 — the disposable-clone helper this task's item 5 points at. Not a prerequisite: item 5's `command -v` degradation must work before 020 lands.

## Target files or areas

- Items 1 to 4: the four Claude-side skill files above, plus codex-side mirrors — and, per the scope delta, `review-cycle/SKILL.md` and `reap-tasks/SKILL.md` (each with its mirror), the inherited-contract and briefless routes by which item 1's remaining reviewer-output gap is reached, and `wf-address-tasks.js`, which carries the workflow-side gaps scope-delta items 2 and 3 name. That set is the floor scope-delta item 1 requires re-deriving, not a closed list.
- Item 5: the seven skills under `plugins/dev-skills/skills/` that spawn a subagent — `address-review`, `address-reviews`, `address-tasks`, `address-tasks-serialized`, `resolve-open-questions`, `review-cycle`, and `reap-tasks` (which spawns without composing a brief, so its boundary text is new rather than an amendment) — plus their codex-side mirrors, and all three workflows under `plugins/dev-skills/workflows/` that compose one: `wf-review-cycle.js` (the preferred single home for the cycle briefs, at the `cycleContract()` call sites rather than in `cycleDefaultContract()` — scope-delta item 4 explains why the distinction decides whether the text propagates — plus its own `scopePrompt`, which sits outside the embeddable section), `wf-address-tasks.js` (whose *cycle* briefs may arrive through its byte-identical embedded `review-cycle-core` section rather than by a separate edit — but only for a placement its own contract overrides do not bypass, and its eight non-cycle briefs need the text directly either way), and `wf-address-review.js` (which nests rather than embeds the cycle, so its own gather and publish briefs need the text directly). Item 5's enumeration is a floor: re-derive the full set from the source before delivering, per the note there.

## Implementation notes

- Keep it terse: one rule statement plus the counterexamples where subagent prompts are specified; do not add a lecture to every section.
- Rendering every prompt path sounds heavier than it is, and the repo already has the technique: `scripts/test-checkout-cleanliness-report.mjs` extracts a function from `wf-address-tasks.js` by regex and evaluates it with `new Function`, precisely because a workflow is a runtime script that cannot be imported. Every `*Prompt`/note function is pure and template-only, so the same extraction renders each one — but extract each **together with the helpers it calls**, which the precedent did not need: `shq` (`wf-address-tasks.js:422`, and its sibling in `wf-address-review.js`) for briefs like `cleanupNote` and `prPrompt`, and the `cycle*` contract and block builders for the cycle prompts. Pulling a function out alone gives a `ReferenceError`, not a rendered prompt. The two cycle configurations are just `cycle.contracts` set versus unset. Do this rather than eyeballing the source — eyeballing is what missed three paths across three review rounds.
- The cleanliness assertion is a REPORT obligation, not an auto-clean: the skill must not instruct `git clean`/`checkout` on the shared main tree (that is exactly the co-tenant hazard powbox's shared-surfaces guidance warns about).

## Acceptance criteria

- No skill documents a fixed-name scratch file for anything a parallel subagent writes. Where a recipe still writes an artifact, its documented name is identity-namespaced (`threads-<PR#>.json`-style). **Writing no artifact at all satisfies item 2 outright** — the thread-fetch recipe now streams `gh-review-threads` to stdout, and a delivery must not reintroduce a dump file in order to have a name to namespace. This criterion is about what a collision could be built on, not about a filename appearing anywhere.
- Batch skills instruct orchestrators to hand each subagent a unique scratch location (or require worktree-local output) in the subagent prompt template. A reviewer told to run a build is told where that build's output goes, on every path that spawns one — by all three routes the delta names: an inline template, an inherited contract (how `address-reviews`' own Reviewer gets its brief), and a spawn instruction carrying no brief at all (`reap-tasks`), which no contract edit reaches and which is therefore verified by reading the rule in the skill text the orchestrator itself follows. Namespacing a peer's paths while leaving the reviewer beside it unplaced does not satisfy this, and neither does covering the two templated routes while the briefless one stays bare.
- `address-tasks` (both variants) ends with the main-checkout cleanliness check and the report-don't-clean rule, and runs it on every batch termination — including zero-delivery and aborted batches — not only when a task delivered. `wf-address-tasks.js` is held to the same criterion rather than credited as already satisfying it: no return taken after the baseline may skip the comparison, which today `:1661` does. It compares against a baseline captured at batch start, so a checkout already dirty at bootstrap does not read as batch leakage and a vanished baseline **path** is surfaced rather than passing silently — keyed by path, so a status-code change (a co-tenant staging their own work) reads as a re-classification rather than as one path vanishing and another appearing.
- The delivered text states the check as a report obligation whose claim excludes what was written to paths already present in the baseline, and paths its reading cannot see, rather than as an assurance the batch left the tree untouched. A text presenting it as a completeness guarantee fails this criterion whatever comparison it prescribes, and one that instead enumerates a closed list of blind spots fails it too — the exclusion is the point, since the list is open.
- The hypothesis-not-finding line is present in the batch skills.
- Every place that spawns a subagent — the seven skills, their mirrors, and all three workflows (`wf-review-cycle.js`, `wf-address-tasks.js`, `wf-address-review.js`) — states the permitted-versus-forbidden command boundary with the "not in a clone, not in a temp directory, not 'safely'" qualifier, states that a worktree isolates the working tree rather than the repository, and directs empirical verification to a disposable clone or an absolute path outside the repository rather than leaving the location to the subagent. A skill that names the rule but still lets the subagent choose the location fails this criterion, and so does one that covers the skills while leaving any workflow's inlined briefs untouched. The criterion is about the text a spawned subagent actually receives, not about the number of files edited, and it is verified by **rendering every distinct prompt path and reading the boundary in each** — never by arguing from the embed relationship, and never by sampling one prompt per workflow. A briefless spawn instruction has no prompt path to render, so it is verified instead by reading the boundary in the skill text that governs the orchestrator composing that prompt: for `reap-tasks`, in both mirrors, beside the instruction that raises the parallel option. Item 5 enumerates the paths known at writing — the four `cycleContract()` call sites, exercised under both a standalone configuration and an overriding batch one since those take different branches; `wf-review-cycle.js`'s own `scopePrompt`; `wf-address-tasks.js`'s eight own briefs, including both branches `prPrompt` can return; and `wf-address-review.js`'s gather and publish briefs — but the criterion is over **the complete set derived from the source at implementation time**, not over that list, which three review rounds each extended and each left short. `wf-address-tasks.js` may satisfy the *cycle* half through the `review-cycle-core` section it embeds byte-for-byte, but only for a placement its own per-role contract overrides do not bypass, and that half says nothing about its own briefs; a byte-identical section proves the text was copied, not that it renders. A delivery fails this criterion if it covers only the two workflows the pre-delta list named, if its text sits in a code path a consumer never reaches, or if it leaves **any** prompt path bare while another passes — whether or not that path is named above.
- `rebase-stack` and `enable-worktrees` are left alone, and a delivery that adds item 5's boundary text to either — as a prompt contract or as a spawn instruction — fails this criterion.

## Validation

- Grep the skills for `threads.json` / `verify.log` / other fixed scratch names — none remain unqualified.
- Walk one `address-reviews` dry run mentally (or live on two disposable PRs) confirming two concurrent fixers cannot share a filename under the documented recipe.

## Review plan

For items 1 to 3, reviewer enumerates every scratch-file mention across the five skills those items now name — `address-tasks`, `address-tasks-serialized`, `address-reviews`, `review-cycle`, and `reap-tasks` — and checks each is either worktree-local or identity-namespaced, and that the cleanliness step cannot be read as license to clean the shared checkout. Reviewer also traces the reviewer-build-output rule down all three routes scope-delta item 1 names, since each is verified differently: an inline template is read directly, an inherited contract is followed through its delegation hop to confirm one edit reaches every consumer, and a briefless spawn instruction — `reap-tasks`, in both mirrors — has no prompt to trace and is verified by reading the rule in the skill text the orchestrator itself follows. As with item 5, reviewer re-derives that set from the source rather than working down the list, since this one has been short at every round so far; a route the implementer covered but the delta never named is a pass, and one neither covered is a finding. Reviewer also walks `wf-address-tasks.js` from the baseline forward to confirm no return reaches the caller without the comparison. For item 4, that the hypothesis-not-finding line is present in the batch skills.

For item 5, reviewer checks that all seven spawning skills, their Codex mirrors, and all three dynamic workflows carry the destroy boundary with its "not in a clone, not in a temp directory, not 'safely'" qualifier and the worktree-isolates-the-checkout-not-the-repository line — established by rendering every prompt path and reading the boundary in each output, rather than by counting edited files, by checking that the embedded section stayed byte-identical, or by sampling one prompt per workflow. Reviewer re-derives the set of paths from the source rather than working down item 5's list, since that list has been short at every previous round; a path the implementer covered but item 5 never named is a pass, and one neither covered is a finding. That re-derivation must include spawn instructions that hand over no brief — `reap-tasks` is the known one, verified by reading the boundary in both mirrors rather than by rendering a prompt it never composes. For `wf-address-tasks.js` in particular, confirm the text survives its own per-role contract overrides *and* reaches its eight non-cycle briefs — `cleanupNote` especially, which sends the subagent to the repo root — and for `wf-address-review.js` that it reaches the gather and publish briefs the nested cycle never touches; that empirical verification is directed to a disposable clone or an absolute path outside the repository rather than left to the subagent to place; and that neither `rebase-stack` nor `enable-worktrees` gained item 5's boundary text in any form.
